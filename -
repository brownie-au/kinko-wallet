                    </tr>                  </thead>                  <tbody>                    {loading && <LoadingRow label={`Loading ${activeChain === 'pulse' ? 'PRC-20' : activeChain === 'all' ? 'ERC-20 & PRC-20' : 'ERC-20'}…`} colSpan={5} />}                    {!loading && err && <tr><td colSpan={5} className="text-danger py-4 text-center">{err}</td></tr>}                    {!loading && !err && items.length === 0 && (                      <tr><td colSpan={5} className="py-4 text-center">No tokens found.</td></tr>                    )}                    {!loading && !err && items.map((t, i) => {                      const price = (t.price ?? t.priceUsd ?? 0);                      const amount = (t.amount ?? 0);