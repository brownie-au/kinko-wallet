// src/sections/dashboard/crypto/default/RatingCard.jsx
// Retired component. Keep the file so old imports don't explode.
// Renders nothing.

export default function RatingCard() {
  if (import.meta?.env?.DEV) {
    console.debug('[RatingCard] noop render (component retired)');
  }
  return null;
}
